#include "main.h"
/**
 * _memcpy - fills memory withanother buffer
 * @dest: source string
 * @src: string for filing
 * @n: length of buffer
 * Return: new string
 */
char *_memcpy(char *dest, char *src, unsigned int n)
{
	unsigned int = 0;

	while (i < 0)
	{
		*(dest + i) = *(src + i);
		i++;
	}
	return (dest);
